/*package com.example.restservice.model;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.GenerationType;
import javax.persistence.Id;
import javax.persistence.JoinColumn;
import javax.persistence.ManyToOne;
import javax.persistence.MapsId;
import javax.persistence.OneToOne;
import javax.persistence.PrimaryKeyJoinColumn;
import javax.persistence.PrimaryKeyJoinColumns;
import javax.persistence.SecondaryTable;
import javax.persistence.SecondaryTables;
import javax.persistence.Table;

import com.example.restservice.model.Utentes;

@Entity
@Table(name="lista_centro")
/*@SecondaryTable(name="centros_vacinacao", 
pkJoinColumns=@PrimaryKeyJoinColumns(name="idCentro", value = { @PrimaryKeyJoinColumn idCentro }))
public class ListaCentro {
	
	
	
	/*@Id
	@Column(name="idCentro")
	private CentroVac centro;
	
	/*@Id
	@Column(name ="idUtente")	
	private long idUtente;
	
	 /*@OneToOne(mappedBy = "lista_centro")
	 private Utentes utente;
	 
	 /*@OneToOne
	 @MapsId
	 @JoinColumn(name = "idUtente")
	 private Utentes utente;
	
	 /*@OneToOne
	 @MapsId
	 @JoinColumn(name = "idUtente")
	 private Utentes utente;
	 
	 @OneToOne
	 @MapsId
	 @JoinColumn(name = "idCentro")
	 private CentroVac centros;
	
	
	
	
	
}*/
